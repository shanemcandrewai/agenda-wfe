# CGI page test
Send every keystroke to server side script and return a new page
## Start local web server in Python 3 virtual environment
    python -m http.server --cgi
## Load web page in browser
    http://localhost:8000
